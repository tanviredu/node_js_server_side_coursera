// every program is a node module
// we make a node module
exports.peremeter = (x,y)=>{return 2*(x+y)};
exports.area = (x,y)=>{return x*y};

// we are exporting the two functionality 
// as we create a node module